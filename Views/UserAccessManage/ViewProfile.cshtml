@model EcdsApp.Models.UserManage.ApplicationUser
@{
	ViewData["Title"] = "Profile Info";	
	string profilePic = Model.ProfilePic;
	if(profilePic != null){
		profilePic = profilePic.Split("wwwroot")[1];
	}
	string dateOfBirth = null;
	if(Model.DateOfBirth != null)
	{
		string[] date = Model.DateOfBirth.Split("/");
		dateOfBirth = $"{date[1]}/{date[0]}/{date[2]}";
	}
	
	
}

@section Styles{
	<style>
	table td{
		font-size:1.25rem;
		font-weight:400;
	}
	tr{
		display:flex;
		justify-content:space-between;
	}
	tr>td:nth-child(1){
		font-weight:600;
	}
	.prof-card{
		box-shadow: rgba(0, 0, 0, 0.07) 0px 1px 2px, rgba(0, 0, 0, 0.07) 0px 2px 4px, rgba(0, 0, 0, 0.07) 0px 4px 8px, rgba(0, 0, 0, 0.07) 0px 8px 16px, rgba(0, 0, 0, 0.07) 0px 16px 32px, rgba(0, 0, 0, 0.07) 0px 32px 64px;
		padding:60px 30px;
		border-radius:10px;

	}
	.vh90{
		min-height:90vh;
	}

	.prof-img{
		height:150px;
		border-radius:50%;
	}
	</style>
}
<div class="page-content bg-white vh90 d-flex justify-content-center align-items-center">
	<div class="container-fluid">
		<div class="row justify-content-center align-items-center">
			<div class="prof-card col-lg-5 col-md-10 col-12">
				<div class="d-flex justify-content-end align-items-center">
					<a asp-action="UpdateProfile" asp-route-userid="@Model.Id"><i class="bx bx-edit fs-5 pe-2"></i>Edit Profile</a>
				</div>
				<div class="d-flex justify-content-center align-items-center">
					@*<i class="py-2 bx bx-lg bx-user-pin"></i>*@
					@if(profilePic == null)
					{
						<img class="prof-img" src="~/assets/profile_pics/user_icon.png" />
					}
					else
					{
						<img class="prof-img" src="@profilePic" />
					}

				</div>
				<h2 class="text-center fs-2 mt-5">Profile Info</h2>
				<table class="w-100 table-responsive mt-5">
				<thead>
					<tr>
						<td></td>
						<td></td>
					</tr>
				</thead>
				<tbody>
					<tr>
						<td>Name</td>
						<td>@Html.Raw(Model.FirstName+" "+Model.LastName)</td>
					</tr>
					<tr>
						<td>Email</td>
						<td>@Model.Email</td>
					</tr>
					<tr>
						<td>Designation</td>
						<td>@Model.Designation</td>
					</tr>
					<tr>
						<td>Contact Number</td>
						<td>@Model.MobileNo</td>
					</tr>
					<tr>
						<td>Address</td>
						<td>@Model.Address</td>
					</tr>
					<tr>
						<td>Date of Birth</td>
						<td>@dateOfBirth</td>
					</tr>
					<tr>
						<td>Name of the Organization</td>
						<td>@Model.Organization</td>
					</tr>
				</tbody>
			</table>
			</div>
		</div>
	</div>
</div>

