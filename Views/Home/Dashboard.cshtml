@using Newtonsoft.Json
@model EcdsApp.Models.ViewModels.Dashboard.DashboardVm
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Home";
    Layout = "~/Views/Shared/_Layout.cshtml";

    var layerData = JsonConvert.SerializeObject(Model.DistrictWisePoverties);
    var legendData = JsonConvert.SerializeObject(Model.LayerLegendColors);

    var xLabels = JsonConvert.SerializeObject(Model.UpazilaWiseExposureData.Select(x => x.Upazila.UpazilaName));
    var yFloodValue = JsonConvert.SerializeObject(Model.UpazilaWiseExposureData.Select(x => x.FloodValue));
    var yLandSlideValue = JsonConvert.SerializeObject(Model.UpazilaWiseExposureData.Select(x => x.LandSlideValue));
    var yDroughtValue = JsonConvert.SerializeObject(Model.UpazilaWiseExposureData.Select(x => x.DroughtValue));

}
@section Styles{

    <link href="~/assets/libs/leaflet/leaflet.css" rel="stylesheet" type="text/css" />
    <link href="~/assets/css/mapstyle.css" rel="stylesheet" />
    <style>
            .legend_info {
                padding: 1px;
                background: rgba(255,255,255,0.8);
                box-shadow: 0 0 15px rgba(0,0,0,0.2);
                border-radius: 5px;
                /*font-size: 1vw;*/
                font: 400 14px/1.0 Roboto, Helvetica, Arial, sans-serif;
                /*overflow: scroll;*/
                min-height: 100px;
                max-height: 400px;
               /* resize: both;*/
            }

                .legend_info i {

                    width: 20px;
                    height: 15px;
                    float: left;
                    margin-right: 10px;
                    opacity: 0.7;
                    font: 400 14px/1.0 Roboto, Helvetica, Arial, sans-serif;
                }

                .legend_info p {
                    padding: 0.2vw;
                }
    </style>
}


<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-6 col-xl-3">
                <div class="card">
                    <div class="card-body">
                        <div class="float-end mt-2">
                            <div id="total-revenue-chart"></div>
                        </div>
                        <div>
                            <h4 class="mb-1 mt-1">$<span data-plugin="counterup">34,152</span></h4>
                            <p class="text-muted mb-0">Total Revenue</p>
                        </div>
                        <p class="text-muted mt-3 mb-0">
                            <span class="text-success me-1"><i class="mdi mdi-arrow-up-bold me-1"></i>2.65%</span> since last week
                        </p>
                    </div>
                </div>
            </div> <!-- end col-->

            <div class="col-md-6 col-xl-3">
                <div class="card">
                    <div class="card-body">
                        <div class="float-end mt-2">
                            <div id="orders-chart"> </div>
                        </div>
                        <div>
                            <h4 class="mb-1 mt-1"><span data-plugin="counterup">5,643</span></h4>
                            <p class="text-muted mb-0">Orders</p>
                        </div>
                        <p class="text-muted mt-3 mb-0">
                            <span class="text-danger me-1"><i class="mdi mdi-arrow-down-bold me-1"></i>0.82%</span> since last week
                        </p>
                    </div>
                </div>
            </div> <!-- end col-->

            <div class="col-md-6 col-xl-3">
                <div class="card">
                    <div class="card-body">
                        <div class="float-end mt-2">
                            <div id="customers-chart"> </div>
                        </div>
                        <div>
                            <h4 class="mb-1 mt-1"><span data-plugin="counterup">45,254</span></h4>
                            <p class="text-muted mb-0">Customers</p>
                        </div>
                        <p class="text-muted mt-3 mb-0">
                            <span class="text-danger me-1"><i class="mdi mdi-arrow-down-bold me-1"></i>6.24%</span> since last week
                        </p>
                    </div>
                </div>
            </div> <!-- end col-->

            <div class="col-md-6 col-xl-3">

                <div class="card">
                    <div class="card-body">
                        <div class="float-end mt-2">
                            <div id="growth-chart"></div>
                        </div>
                        <div>
                            <h4 class="mb-1 mt-1">+ <span data-plugin="counterup">12.58</span>%</h4>
                            <p class="text-muted mb-0">Growth</p>
                        </div>
                        <p class="text-muted mt-3 mb-0">
                            <span class="text-success me-1"><i class="mdi mdi-arrow-up-bold me-1"></i>10.51%</span> since last week
                        </p>
                    </div>
                </div>
            </div> <!-- end col-->
        </div> <!-- end row-->

        <div class="row">
            <div class="col-xl-6">
                <div class="card">
                    <div class="card-body">

                        <div class="mt-3">
                            <div id="exposure_chart" class="apex-charts" dir="ltr"></div>
                        </div>
                    </div> <!-- end card-body-->
                </div> <!-- end card-->
            </div> <!-- end col-->

            <div class="col-xl-6">
                <div class="card">
                    <div class="card-body">

                        <div id="dashboard_map" class="text-center" style="min-height:600px; padding:0.75rem;">
                            <div class="spinner-grow text-info" style="width:5rem; height:5rem; margin-top:4.5rem;" role="status">
                                <span class="sr-only">Loading... . .</span>
                            </div>
                        </div>
                    </div>
                </div>

            </div> <!-- end Col -->
        </div> <!-- end row-->
        <!-- end row -->

        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title mb-4">Latest Five Layer List</h4>
                        <div class="table-responsive">
                            <table id="tblLayerData" class="table table-centered table-nowrap mb-0">
                                <thead class="table-light">
                                    <tr class="bg-transparent">
                                        <th>Theme Name</th>
                                        <th>Sub-theme Name</th>
                                        <th>Layer Name</th>
                                        <th>Layer Type</th>
                                        <th>Boundary</th>
                                        <th>Tabular Data</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.ThemeLayerDetails)
                                    {
                                        <tr>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.SubThemes.Themes.ThemeName)
                                            </td>

                                            <td>
                                                @Html.DisplayFor(modelItem => item.SubThemes.SubThemeName)
                                            </td>

                                            <td>
                                                @Html.DisplayFor(modelItem => item.LayerName)
                                            </td>

                                            <td>@item.ThemeLayerTypes?.LayerTypeName</td>
                                            <td>@item.BoundaryInfo?.BoundaryName</td>
                                            <td>
                                                <a class="badge bg-pill bg-soft-info font-size-14" target="_blank" asp-controller="@item.TableInfo?.TableModelName" asp-action="Index" style="color: #191c1e">
                                                    @Html.DisplayFor(modelItem => item.TableInfo.DisplayName)
                                                </a>
                                            </td>


                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                        <!-- end table-responsive -->
                    </div>
                </div>
            </div>
        </div>
        <!-- end row -->
    </div>
</div>

@section Scripts{
    <!-- apexcharts -->
    <script src="~/assets/libs/leaflet/leaflet.js"></script>
    <script src="~/assets/libs/apexcharts/apexcharts.min.js"></script>
    @*<script src="~/assets/js/pages/dashboard.init.js"></script>*@
    <script src="~/assets/js/pages/dashboard.init.js"></script>

    <script type="text/javascript">
        var dashboard_map;
        var legend = L.control({ position: 'topright' });
        var legend_div = L.DomUtil.create('div', 'legend_info');
        var legendcolorField = "DistGeoCode";
        mapInit();
        addLayer();
        var legend_data = [];
        var layer_Data = [];

        function mapInit() {
            var openstreet = L.tileLayer("http://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", { id: 'MapID', minZoom: 0, maxZoom: 25 }),
                blank = L.tileLayer("../images/blank.png", { id: 'MapID', minZoom: 0, maxZoom: 25 }
                );


            dashboard_map = new L.Map("dashboard_map",
                {
                    center: new L.LatLng(23.737777, 90.537777),
                    fullscreenControl: true,
                    zoomControl: false,
                    maxZoom: 20,
                    minZoom: 2,
                    zoom: 6.0,
                    layers: [openstreet],

                });
            dashboard_map.setView([23.837777, 90.347777], 7.0);
            L.control.zoom({
                position: 'bottomleft'
            }).addTo(dashboard_map);



        }

        function addLayer() {
            var layerPath = "../ecds/assets/js/map/map_data/administrative/administrative_boundary/district/district.json";
            var layerData = L.geoJson(null, {
                style: style,
                onEachFeature: onEachFeature,
            });


            $.getJSON(layerPath, function (LayerInfo) {
                if (!LayerInfo) return;
                layerData.addData(LayerInfo);
            }).done(function (e) {
                dashboard_map.addLayer(layerData);
            });
            function style(feature) {
                var color = getColor(feature.properties[legendcolorField]);
                return {
                    fillColor: color,
                    weight: 1,
                    opacity: 0.5,
                    /* transparent: true,*/
                    color: color,
                    /*dashArray: '1',*/
                    fillOpacity: 0.9
                };
            }
            function onEachFeature(feature, layer) {
                layer.on({
                    mouseover: highlightFeature,
                    mouseout: resetHighlight,
                });
            }
            function highlightFeature(e) {
                var layer = e.target;
                var popupContent = '<div style="width:300px;text-align:center;"><p>' + layer.feature.properties["DistGeoCod"] + '</p></div>';
                layer.bindPopup(popupContent).openPopup(e.latlng);

                //layer.setStyle({
                //    //weight: 2,
                //    //color: '#E11F21',
                //    dashArray: '',
                //    //fillOpacity: 0.4
                //}).bindPopup(popupContent).openPopup(e.latlng);

                //if (!L.Browser.ie && !L.Browser.opera && !L.Browser.edge) {
                //    layer.bringToFront();
                //}
            }
            function resetHighlight(e) {
                dashboard_map.closePopup();
                layerData.resetStyle(e.target);
            }

            legend.onAdd = function (map) {
                legend_data =@Html.Raw(legendData);
                legend_div.innerHTML += '<p>Poverty Rate </p><hr/>';

                for (var i = 0; i<legend_data.length; i++) {

                    legend_div.innerHTML += '<p class="">' + legend_data[i].LayerLegendDisplayName + '<i style="background:' + legend_data[i].LayerLegendColorCode + '"></i> </p>';
                }
                return legend_div;
            };
            legend.addTo(dashboard_map);
        }

        function getColor(d) {

            //console.log(userDefinedColorList["main_river"][0].get('color'));
            //LayerMainAttribureValue

            for (var i = 0; i < legend_data.length; i++) {
                if (d >= legend_data[i].LayerMainAttribureValue && d < legend_data[i+1].LayerMainAttribureValue) {
                    return legend_data[i].LayerLegendColorCode;
                }
                else if (d >= legend_data[legend_data.length - 1].LayerMainAttribureValue) {
                    return legend_data[legend_data.length - 1].LayerLegendColorCode;
                }
            }
        }
        function getMapData() {

        }



        $(function () {
            function groupTable($rows, startIndex, total) {
                if (total === 0) {
                    return;
                }
                var i, currentIndex = startIndex, count = 1, lst = [];
                var tds = $rows.find('td:eq(' + currentIndex + ')');
                var ctrl = $(tds[0]);
                lst.push($rows[0]);
                for (i = 1; i <= tds.length; i++) {
                    if (ctrl.text() === $(tds[i]).text()) {
                        count++;
                        $(tds[i]).addClass('deleted');
                        lst.push($rows[i]);
                    } else {
                        if (count > 1) {
                            ctrl.attr('rowspan', count);
                            groupTable($(lst), startIndex + 1, total - 1);
                        }
                        count = 1;
                        lst = [];
                        ctrl = $(tds[i]);
                        lst.push($rows[i]);
                    }
                }
            }

            groupTable($('#tblLayerData tr:has(td)'), 0, 2);
            $('#tblLayerData .deleted').remove();
        });

        var options = {
            chart: { height: 339, type: "line", stacked: !1, toolbar: { show: !1 } },
            stroke: { width: [0, 2, 4], curve: "smooth" },
            plotOptions: { bar: { columnWidth: "30%" } },
            colors: ["#5b73e8", "#dfe2e6", "#f1b44c"],
            series: [{ name: "Flood", type: "column", data: @Html.Raw(yFloodValue) },
                { name: "Land Slide", type: "area", data: @Html.Raw(yLandSlideValue) },
                { name: "Drought", type: "line", data: @Html.Raw(yDroughtValue) }],
                //{ name: "Drought", type: "line", data: [5, 11, 21, 2, 14, 6, 18, 25, 15, 10, 9, 10] }],
            fill: {
                opacity: [.85, .25, 1],
                gradient: { inverseColors: !1, shade: "light", type: "vertical", opacityFrom: .85, opacityTo: .55, stops: [0, 100, 100, 100] }
            },
            labels: @Html.Raw(xLabels),
            markers: { size: 0 },
            xaxis: { title: { text: "Upazila" } },
            yaxis: { title: { text: "Value" } },
            tooltip: {
                shared: !0, intersect: !1,
                y: { formatter: function (e) { return void 0 !== e ? e.toFixed(0) + " value" : e } }
            },
            grid: { borderColor: "#f1f1f1" }
        };

        (chart = new ApexCharts(document.querySelector("#exposure_chart"), options)).render();






    </script>
}